# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
# local env files
.env*.local
.env
.env.example

# API keys - Keep example files but ignore the actual keys
# src/lib/api-keys.example.ts is committed (template)
# src/lib/api-keys.ts is tracked and loads from .env
# .env.example is committed (template)
# .env is ignored (contains actual keys)

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
